"use client";

// Re-export AppSidebar as Navbar for the layout barrel — keeps naming consistent
// with the file assignment doc which lists both Navbar.tsx and AppSidebar.tsx.
export { default } from "@/components/sidebar/AppSidebar";
